<h1>All our Tiny Houses</h1>

<div class="cards">
  <% @tiny_houses.each do |tiny| %>
    <h2><%= tiny.title %></h2>
    <p><%= tiny.description %></p>
    <p><%= link_to 'show', tiny_house_path(tiny.id) %></p>
  <% end %>
</div>
